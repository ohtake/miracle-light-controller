{
  "name": "miracle-light-controller",
  "version": "1.0.0",
  "license": "ISC",
  "main": "src/mlc.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "run-p -l build:*",
    "build:static": "copyfiles ./static/* --up 1 dist/",
    "build:tsc": "tsc"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "npm-run-all2": "^6.0.5",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
